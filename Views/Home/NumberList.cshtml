@{
    ViewData["Title"] = " ";
}
<h1>@ViewData["Title"]</h1>

<p>
    <div class="container">
      <h1 class="mt-3">Number List</h1>

      <div class="row mt-3">
        <input
          class="form-control"
          type="text"
          placeholder="Sayı giriniz"
          id="number"
        />
      </div>

      <ul class="list-group mt-3" id="list"></ul>

      <button class="btn btn-success" id="btn-add" onclick="addNumber()">
        +
      </button>
    </div>
</p>
